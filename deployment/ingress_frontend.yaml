---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: words-frontend
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  tls:
    - hosts:
        - words.example.com
      secretName: words-frontend-tls
  rules:
    - host: words.example.com
      http:
        paths:
          - pathType: Prefix
            path: "/"
            backend:
              service:
                name: words-frontend
                port:
                  number: 80
